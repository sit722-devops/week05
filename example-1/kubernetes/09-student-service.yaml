apiVersion: apps/v1
kind: Deployment
metadata:
  name: student-service
  namespace: default
spec:
  replicas: 1

  selector:
    matchLabels:
      app: student-service

  template:
    metadata:
      labels:
        app: student-service

    spec:
      initContainers:
        - name: wait-for-student-db
          image: postgres:16-alpine
          command:
            - sh
            - -c
            - |
              until pg_isready \
                -h student-db \
                -p 5432 \
                -U postgres \
                -d students;
              do
                echo "Waiting for student database..."
                sleep 3
              done

      containers:
        - name: student-service
          image: koalatech-student-service:v1
          imagePullPolicy: IfNotPresent

          ports:
            - containerPort: 8000

          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: koalatech-application-secret
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: koalatech-application-secret
                  key: POSTGRES_PASSWORD

            - name: POSTGRES_DB
              value: students

            - name: POSTGRES_HOST
              value: student-db

            - name: POSTGRES_PORT
              value: "5432"

            - name: JWT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: koalatech-application-secret
                  key: JWT_SECRET_KEY

            - name: JWT_ALGORITHM
              value: HS256

            - name: AZURE_STORAGE_CONNECTION_STRING
              valueFrom:
                secretKeyRef:
                  name: koalatech-application-secret
                  key: AZURE_STORAGE_CONNECTION_STRING

            - name: AZURE_STORAGE_CONTAINER_NAME
              value: student-profile-photos

          readinessProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 5

          livenessProbe:
            tcpSocket:
              port: 8000
            initialDelaySeconds: 20
            periodSeconds: 10

---
apiVersion: v1
kind: Service
metadata:
  name: student-service
  namespace: default
spec:
  type: ClusterIP

  selector:
    app: student-service

  ports:
    - name: http
      port: 8000
      targetPort: 8000